console.log("Currying");

function sum(a, b, c) {
	// calculates the sum of all the numbers int he array nums[]
}

// sum(2, 3,4);
curry_sum(2)(3, 4)
curry_sum(2)(3)(4);

function curry(fn) {
	return //
}

import _ from 'lodash';
